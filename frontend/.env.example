# Where the dev server sends API calls.
#   http://localhost:8787  -> Cloudflare Worker (wrangler dev, in ../worker)
#   http://localhost:8000  -> legacy FastAPI backend (uvicorn, in ../backend)
# Production builds ignore this file (.env.production pins same-origin).
REACT_APP_BACKEND_URL=http://localhost:8787
