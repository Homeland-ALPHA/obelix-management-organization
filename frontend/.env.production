# Production builds call the API on the same origin — the Cloudflare Worker
# serves the static app and /api together. Set a URL here only for split
# deployments (frontend and API on different origins).
REACT_APP_BACKEND_URL=
